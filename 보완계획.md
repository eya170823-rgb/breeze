# 브리즈 홈페이지 보완계획

> 작성 2026-08-27 · 대상 https://eya170823-rgb.github.io/breeze/
> 프로젝트 폴더 `G:\내 드라이브\07_자동화프롬테크\07_홈페이지만들기`

---

## 1. 오늘 한 일 (커밋 `9525037`)

실제 CRM 피드(매물 23건)로 돌려서 확인한 결과까지 함께 적습니다.

| # | 고친 것 | 왜 | 확인된 결과 |
|---|---------|-----|------------|
| 1 | **매물사진 연동 준비** | 사진 정리가 끝나면 CRM 한 줄만 고쳐도 바로 뜨게 | 드라이브 링크→표시가능 주소 변환·여러 장 처리 모두 통과 |
| 2 | **가짜 샘플 매물 삭제** | 피드 실패 시 지어낸 매물이 진짜처럼 노출됐음 | `sample-data.js` 제거, 실패 시 "불러오지 못했습니다 + 전화하기" |
| 3 | **가격 필터 개편** | 월세 기준값이 '보증금'이라 필터가 사실상 없었음 | 월세 20건이 `월30~50` 13건 / `월50~80` 7건으로 분리 |
| 4 | **빈 카테고리 비활성** | 아파트·오피스텔·상가임대가 빈 화면을 열었음 | 원투룸만 활성(20 뱃지), 나머지 3개 회색·클릭 불가 |
| 5 | **미완성 매물 숨김(임시)** | 매물장 입력분이 그대로 다 올라가는 구조 | 23건 → 20건 노출 / 3건 숨김 |

### 1-1. 사진 — 지금 어디까지 돼 있나

홈페이지 쪽은 **다 준비됐습니다.** 남은 건 CRM 한 곳뿐입니다.

- 받는 칸 이름은 `image` / `images` / `photo` / `photos` 아무거나 됩니다.
- 쉼표나 줄바꿈으로 **여러 장**을 보내면 목록엔 대표 1장 + `📷 3` 장수 뱃지,
  상세를 열면 옆으로 넘겨보는 사진 띠가 나옵니다.
- **구글 드라이브 공유링크를 그대로 보내도 됩니다.** 홈페이지가 알아서
  화면에 뜨는 주소로 바꿉니다. (단 그 파일이 '링크가 있는 모든 사용자에게 공개'여야 보입니다)

**남은 한 줄** — CRM `google_apps_script.js`의 `getHomepageListings()` 안:

```js
image: '',                       // ← 지금 (사진이 절대 안 나감)
image: g('photoUrl'),            // ← 이렇게 바꾸면 끝
```

⚠️ 주의: `photoUrl`은 지금 **사진이 든 '폴더' 링크**입니다. 폴더 링크는 사진으로 안 보입니다.
'보정완료' 폴더 안 **파일 각각의 공유링크**를 쉼표로 이어 붙여 보내야 합니다.
(CRM에 이미 있는 `drivePhotos` 기능이 파일 목록을 뽑아 주므로 그걸 재활용하면 됩니다.)

### 1-2. 숨겨진 매물 3건 — 지금 바로 살릴 수 있습니다

| 매물번호 | 이름 | 빠진 정보 |
|---|---|---|
| X260814-01 | 연동 더존빌 | 물건유형(kind), 면적, 주소 |
| X260814-02 | 연동 더존빌 | 물건유형(kind), 면적, 주소 |
| X260814-03 | 노형동 라메종하우스 | 물건유형(kind), 면적, 주소 |

CRM 매물관리에서 이 3건의 **물건유형·면적·주소**만 채우면 다음 새로고침에 자동으로 올라갑니다.
(숨긴 매물 목록은 홈페이지에서 `F12` → Console 에도 찍힙니다)

---

## 2. 5번 근본 해결 — CRM 광고관리에 '홈페이지'를 넣기

### 지금 왜 다 올라가나

CRM 광고관리는 이미 **플랫폼별로 켜고 끄는** 구조입니다.

```
당근 · 네이버 · 이실장 · 직방 · 다방 · 피터팬 · 블로그   ← 광고관리에서 ON/OFF
홈페이지                                            ← 여기에 없음 (그래서 전부 자동 노출)
```

홈페이지만 이 체계 밖에 있어서, 매물관리 시트를 통째로 읽고
"거래유형 + 가격 + 위치가 있으면 공개"라는 **최소 조건**만 봅니다.
즉 **사장님이 '이건 홈페이지에 올리자'고 결정한 적이 한 번도 없습니다.**
오늘 넣은 '미완성 숨김'은 그 구멍을 임시로 막은 것일 뿐입니다.

### 어떻게 고치나 — 플랫폼 하나 추가

CRM `index.html`의 `PLATFORMS` 배열에 한 줄:

```js
{ key:'homepage', label:'홈페이지', icon:'🏠', color:'#bd6a52',
  bulk:false, dedup:'listing', validDays:0 }
```

그리고 `getHomepageListings()`가 **`adsFlat`에 `homepage~`가 있는 매물만** 내보내게 합니다.

### 이렇게 하면 따라오는 것들 (공짜로)

광고관리가 이미 갖고 있는 기능이 홈페이지에도 그대로 적용됩니다.

- **켠 것만 나간다** — 미완성 매물이 새어나갈 구멍이 사라집니다.
- **정보변경 감지(`adStale`)** — 시트에서 가격·면적을 고치면 광고관리에
  "홈페이지에 올린 내용과 달라졌음"이 표시됩니다. 지금은 이런 게 없습니다.
- **게시일·게시 이력** — 언제 홈페이지에 올렸는지 기록이 남습니다.
- **사진 없는 매물 차단** — 사진이 없으면 홈페이지 ON이 안 되게 막을 수 있습니다.
- **다른 플랫폼과 한 화면** — 당근·네이버 옆에 홈페이지가 같이 보입니다.

### 전환 방법

지금 홈페이지에 잘 나가고 있는 **20건을 광고관리에서 일괄로 '홈페이지 ON'** 하면
화면에 보이는 건 그대로 두고 구조만 바뀝니다. 손님 눈에는 변화가 없습니다.

---

## 3. 차례대로 할 일

| 순서 | 할 일 | 어디를 고치나 | 효과 |
|---|---|---|---|
| ✅ **1단계** | 오늘 한 4+1건 | 홈페이지 | 완료 (아직 인터넷엔 안 올림) |
| **2단계** | **매물사진 연동** | CRM 1곳 | **제일 효과 큼.** 사진 없는 매물은 클릭이 안 됩니다 |
| **3단계** | 광고관리에 '홈페이지' 추가 | CRM | 5번 근본 해결 + 정보변경 감지 |
| 4단계 | 매물 상세 주소(URL) 만들기 | 홈페이지 | 지금은 매물 20건인데 **검색에 잡히는 매물 페이지가 0개**. 카톡 공유도 안 됨 |
| 5단계 | 카톡 공유 이미지 · 카카오 상담 링크 · 네이버 플레이스 | 설정값 | 문의가 실제로 들어오는 통로 |
| 6단계 | PC 문의 수단 · 방문자 분석 | 홈페이지 | PC에선 전화 버튼이 동작하지 않음 / 뭘 보고 나가는지 알 수 있음 |

### 🔒 그 전에 한 가지 — 저장소 안전장치

프로젝트 폴더에 **전자책 원고 전체·강의계획서·스크린샷 76장·11MB 동영상**이
`.gitignore`에 안 걸린 채로 있습니다. `git add -A` 한 번이면 **전자책 원고가 통째로
공개 저장소에 올라갑니다.** 아래를 `.gitignore`에 넣어두는 걸 권합니다.

```
전자책*
사진/
아이콘/
안내서/*.pdf
*.docx
*.mp4
_*.py
_*.html
```

### 📁 구글 드라이브로 옮긴 뒤 주의

`.git` 폴더가 드라이브 동기화를 타면 **동기화 충돌로 저장소가 깨질 수 있습니다.**
드라이브 설정에서 `.git`을 동기화 제외로 두거나, 작업은 로컬에서 하고
드라이브는 보관용으로 쓰는 편이 안전합니다.

---

## 4. 지금 인터넷 사이트 상태

오늘 고친 내용은 **아직 인터넷에 올리지 않았습니다** (내 컴퓨터에만 저장됨).
올리려면 `git push` 한 번이면 되고, 1~2분 뒤 사이트에 반영됩니다.
